<!DOCTYPE html>
<html>
  <head>
    <title>Metahub</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= 'bg-warning-subtle' if current_user && current_user.admin? %>">
    <%= render "layouts/navbar" %>
    <div id="wrapper" class="bg-white container shadow-sm border border-top-0 rounded-bottom border-opacity-10 p-3 mb-3">
      <%= render "layouts/flash" %>
      <%= render "layouts/page_header" %>
      <%= yield %>
    </div>

    <footer class="text-center text-secondary fs-6 fw-blod mb-3">
      Copyright &copy; 2023
    </footer>
  </body>
</html>
